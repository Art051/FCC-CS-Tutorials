namespace FCC_CS_Full_Course
{
    class StaticAttributes
    {
        public void PrintAtt()
        {
            Console.WriteLine(Song.songCount);
            //an attribute which doesn't belong to the instance of a class
            //it belongs to the class itself, cannot be changed by instance objects created

        }


    }
}
